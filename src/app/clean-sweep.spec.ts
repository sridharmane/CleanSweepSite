/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {CleanSweep} from './clean-sweep';

describe('CleanSweep', () => {
  it('should create an instance', () => {
    expect(new CleanSweep()).toBeTruthy();
  });
});
